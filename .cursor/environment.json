{
  "name": "gamya-couture-dev",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "install": "bash .cursor/scripts/install.sh",
  "start": "sudo service docker start 2>/dev/null || sudo systemctl start docker 2>/dev/null || true",
  "repositoryDependencies": [
    "github.com/gvsharma/gamya-couture-infra"
  ],
  "ports": [
    { "name": "api", "port": 8080 },
    { "name": "frontend", "port": 3000 },
    { "name": "postgres", "port": 5432 }
  ],
  "terminals": [
    {
      "name": "postgres",
      "command": "docker compose up postgres",
      "description": "Local PostgreSQL (gamya/gamya_secret, db gamya_couture)"
    },
    {
      "name": "api",
      "command": "bash -lc 'until docker compose exec -T postgres pg_isready -U gamya -d gamya_couture >/dev/null 2>&1; do sleep 2; done; mvn spring-boot:run -Dspring-boot.run.profiles=local'",
      "description": "Spring Boot API on :8080 with Flyway local seed"
    },
    {
      "name": "frontend",
      "command": "bash -lc 'cd frontend && npm run dev'",
      "description": "Next.js dev server on :3000"
    }
  ]
}
