name: Deploy Backend to EC2

on:
  push:
    branches:
      - main

concurrency:
  group: deploy-backend-production
  cancel-in-progress: false

env:
  JAVA_VERSION: "21"
  AWS_REGION: ap-south-1
  APP_PATH: /opt/gamya-couture
  DEPLOY_OBJECT_KEY: incoming/gamya-couture.jar

jobs:
  build-and-deploy:
    name: Build, test, and deploy
    runs-on: ubuntu-latest
    timeout-minutes: 30

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: temurin
          cache: maven

      - name: Build and test
        run: mvn -B -ntp verify

      - name: Resolve JAR path
        id: jar
        run: |
          JAR_PATH=$(find target -maxdepth 1 -name 'gamya-couture-*.jar' ! -name '*-original.jar' | head -1)
          if [[ -z "$JAR_PATH" || ! -f "$JAR_PATH" ]]; then
            echo "Executable JAR not found under target/" >&2
            exit 1
          fi
          echo "path=${JAR_PATH}" >> "$GITHUB_OUTPUT"
          echo "Resolved JAR: ${JAR_PATH} ($(du -h "$JAR_PATH" | cut -f1))"

      - name: Validate deploy configuration
        env:
          AWS_BACKEND_DEPLOY_ROLE_ARN: ${{ secrets.AWS_BACKEND_DEPLOY_ROLE_ARN }}
          DEPLOY_BUCKET: ${{ vars.DEPLOY_BUCKET }}
          EC2_INSTANCE_ID: ${{ vars.EC2_INSTANCE_ID }}
          EC2_HOST: ${{ vars.EC2_HOST }}
        run: |
          set -euo pipefail
          missing=()
          [[ -z "${AWS_BACKEND_DEPLOY_ROLE_ARN//[[:space:]]/}" ]] && missing+=(AWS_BACKEND_DEPLOY_ROLE_ARN)
          [[ -z "${DEPLOY_BUCKET//[[:space:]]/}" ]] && missing+=(DEPLOY_BUCKET)
          [[ -z "${EC2_INSTANCE_ID//[[:space:]]/}" ]] && missing+=(EC2_INSTANCE_ID)
          [[ -z "${EC2_HOST//[[:space:]]/}" ]] && missing+=(EC2_HOST)
          if [[ ${#missing[@]} -gt 0 ]]; then
            echo "::error title=Missing deploy config::Set GitHub secret AWS_BACKEND_DEPLOY_ROLE_ARN and variables DEPLOY_BUCKET, EC2_INSTANCE_ID, EC2_HOST (see gamya-couture-infra: terraform output backend_deploy_github_setup)." >&2
            exit 1
          fi

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_BACKEND_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Upload JAR to S3
        env:
          DEPLOY_BUCKET: ${{ vars.DEPLOY_BUCKET }}
        run: |
          set -euo pipefail
          aws s3 cp "${{ steps.jar.outputs.path }}" "s3://${DEPLOY_BUCKET}/${DEPLOY_OBJECT_KEY}"
          echo "Uploaded to s3://${DEPLOY_BUCKET}/${DEPLOY_OBJECT_KEY}"

      - name: Deploy on EC2 via SSM
        env:
          DEPLOY_BUCKET: ${{ vars.DEPLOY_BUCKET }}
          EC2_INSTANCE_ID: ${{ vars.EC2_INSTANCE_ID }}
        run: |
          set -euo pipefail
          cat > /tmp/ssm-deploy-params.json <<EOF
          {
            "commands": [
              "aws s3 cp s3://${DEPLOY_BUCKET}/${DEPLOY_OBJECT_KEY} ${APP_PATH}/incoming/gamya-couture.jar.new",
              "APP_PATH=${APP_PATH} bash ${APP_PATH}/scripts/remote-deploy.sh"
            ]
          }
          EOF

          COMMAND_ID=$(aws ssm send-command \
            --instance-ids "${EC2_INSTANCE_ID}" \
            --document-name "AWS-RunShellScript" \
            --comment "GitHub Actions backend deploy" \
            --parameters file:///tmp/ssm-deploy-params.json \
            --query 'Command.CommandId' --output text)
          echo "SSM command id: ${COMMAND_ID}"

          for attempt in $(seq 1 60); do
            STATUS=$(aws ssm get-command-invocation \
              --command-id "${COMMAND_ID}" \
              --instance-id "${EC2_INSTANCE_ID}" \
              --query 'Status' --output text 2>/dev/null || echo "Pending")
            echo "SSM status: ${STATUS} (attempt ${attempt}/60)"
            case "${STATUS}" in
              Success)
                aws ssm get-command-invocation \
                  --command-id "${COMMAND_ID}" \
                  --instance-id "${EC2_INSTANCE_ID}" \
                  --query '[StandardOutputContent,StandardErrorContent]' \
                  --output text
                exit 0
                ;;
              Failed|Cancelled|TimedOut)
                aws ssm get-command-invocation \
                  --command-id "${COMMAND_ID}" \
                  --instance-id "${EC2_INSTANCE_ID}" \
                  --output json
                exit 1
                ;;
            esac
            sleep 10
          done
          echo "SSM deploy timed out" >&2
          exit 1

      - name: Verify public health via nginx
        env:
          EC2_HOST: ${{ vars.EC2_HOST }}
        run: |
          HEALTH_URL="http://${EC2_HOST}/actuator/health"
          echo "Checking ${HEALTH_URL}"
          for attempt in $(seq 1 12); do
            if curl -sf "$HEALTH_URL" | grep -q '"status":"UP"'; then
              echo "Public health check passed"
              exit 0
            fi
            echo "Attempt ${attempt}/12 — waiting..."
            sleep 5
          done
          echo "Public health check failed" >&2
          exit 1
