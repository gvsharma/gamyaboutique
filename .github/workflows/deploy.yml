name: Deploy Backend to EC2

on:
  push:
    branches:
      - main

concurrency:
  group: deploy-backend-production
  cancel-in-progress: false

env:
  JAVA_VERSION: "21"
  AWS_REGION: ap-south-1
  APP_PATH: /opt/gamya-couture
  DEPLOY_OBJECT_KEY: incoming/gamya-couture.jar
  DEPLOY_SCRIPT_KEY: incoming/remote-deploy.sh

jobs:
  build-and-deploy:
    name: Build, test, and deploy
    runs-on: ubuntu-latest
    timeout-minutes: 30

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: temurin
          cache: maven

      - name: Build and test
        run: mvn -B -ntp verify

      - name: Resolve JAR path
        id: jar
        run: |
          JAR_PATH=$(find target -maxdepth 1 -name 'gamya-couture-*.jar' ! -name '*-original.jar' | head -1)
          if [[ -z "$JAR_PATH" || ! -f "$JAR_PATH" ]]; then
            echo "Executable JAR not found under target/" >&2
            exit 1
          fi
          echo "path=${JAR_PATH}" >> "$GITHUB_OUTPUT"
          echo "Resolved JAR: ${JAR_PATH} ($(du -h "$JAR_PATH" | cut -f1))"

      - name: Resolve deploy configuration
        id: deploy_cfg
        env:
          AWS_BACKEND_DEPLOY_ROLE_ARN: ${{ secrets.AWS_BACKEND_DEPLOY_ROLE_ARN }}
          DEPLOY_BUCKET_VAR: ${{ vars.DEPLOY_BUCKET }}
          DEPLOY_BUCKET_SECRET: ${{ secrets.DEPLOY_BUCKET }}
          EC2_INSTANCE_ID_VAR: ${{ vars.EC2_INSTANCE_ID }}
          EC2_INSTANCE_ID_SECRET: ${{ secrets.EC2_INSTANCE_ID }}
          EC2_HOST_VAR: ${{ vars.EC2_HOST }}
          EC2_HOST_SECRET: ${{ secrets.EC2_HOST }}
        run: |
          set -euo pipefail
          pick_non_empty() {
            local a="$1"
            local b="$2"
            if [[ -n "${a//[[:space:]]/}" ]]; then
              echo "$a"
            else
              echo "$b"
            fi
          }

          DEPLOY_BUCKET="$(pick_non_empty "$DEPLOY_BUCKET_VAR" "$DEPLOY_BUCKET_SECRET" | tr -d '[:space:]')"
          EC2_INSTANCE_ID="$(pick_non_empty "$EC2_INSTANCE_ID_VAR" "$EC2_INSTANCE_ID_SECRET" | tr -d '[:space:]')"
          EC2_HOST="$(pick_non_empty "$EC2_HOST_VAR" "$EC2_HOST_SECRET" | tr -d '[:space:]')"

          missing=()
          [[ -z "${AWS_BACKEND_DEPLOY_ROLE_ARN//[[:space:]]/}" ]] && missing+=(AWS_BACKEND_DEPLOY_ROLE_ARN)
          [[ -z "${DEPLOY_BUCKET//[[:space:]]/}" ]] && missing+=(DEPLOY_BUCKET)
          [[ -z "${EC2_INSTANCE_ID//[[:space:]]/}" ]] && missing+=(EC2_INSTANCE_ID)
          [[ -z "${EC2_HOST//[[:space:]]/}" ]] && missing+=(EC2_HOST)
          if [[ ${#missing[@]} -gt 0 ]]; then
            echo "::error title=Missing deploy config::In gvsharma/gamyaboutique → Settings → Secrets and variables → Actions, set secret AWS_BACKEND_DEPLOY_ROLE_ARN and either Variables or Secrets for DEPLOY_BUCKET, EC2_INSTANCE_ID, EC2_HOST. Values: terraform output backend_deploy_github_setup in gamya-couture-infra." >&2
            exit 1
          fi

          echo "deploy_bucket=${DEPLOY_BUCKET}" >> "$GITHUB_OUTPUT"
          echo "ec2_instance_id=${EC2_INSTANCE_ID}" >> "$GITHUB_OUTPUT"
          echo "ec2_host=${EC2_HOST}" >> "$GITHUB_OUTPUT"

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_BACKEND_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Upload deploy artifacts to S3
        run: |
          set -euo pipefail
          DEPLOY_BUCKET="${{ steps.deploy_cfg.outputs.deploy_bucket }}"
          aws s3 cp "${{ steps.jar.outputs.path }}" "s3://${DEPLOY_BUCKET}/${DEPLOY_OBJECT_KEY}"
          aws s3 cp deploy/scripts/remote-deploy.sh "s3://${DEPLOY_BUCKET}/${DEPLOY_SCRIPT_KEY}"
          echo "Uploaded JAR to s3://${DEPLOY_BUCKET}/${DEPLOY_OBJECT_KEY}"
          echo "Uploaded script to s3://${DEPLOY_BUCKET}/${DEPLOY_SCRIPT_KEY}"

      - name: Deploy on EC2 via SSM
        run: |
          set -euo pipefail
          DEPLOY_BUCKET="${{ steps.deploy_cfg.outputs.deploy_bucket }}"
          EC2_INSTANCE_ID="${{ steps.deploy_cfg.outputs.ec2_instance_id }}"

          EC2_STATE=$(aws ec2 describe-instances \
            --instance-ids "${EC2_INSTANCE_ID}" \
            --query 'Reservations[0].Instances[0].State.Name' \
            --output text 2>/dev/null || echo "unknown")
          echo "EC2 state for ${EC2_INSTANCE_ID}: ${EC2_STATE}"

          PING_STATUS=$(aws ssm describe-instance-information \
            --filters "Key=InstanceIds,Values=${EC2_INSTANCE_ID}" \
            --query 'InstanceInformationList[0].PingStatus' \
            --output text 2>/dev/null || true)
          PING_STATUS="${PING_STATUS:-Unknown}"
          echo "SSM PingStatus for ${EC2_INSTANCE_ID}: ${PING_STATUS}"

          if [[ "${EC2_STATE}" == "stopped" ]]; then
            echo "::error::EC2 instance ${EC2_INSTANCE_ID} is stopped. Start it in AWS Console (ap-south-1) or wait for the cost scheduler (starts 09:00 IST), then re-run deploy." >&2
            exit 1
          fi
          if [[ "${EC2_STATE}" == "unknown" || "${EC2_STATE}" == "None" ]]; then
            echo "::error::EC2 instance ${EC2_INSTANCE_ID} not found in ap-south-1. Fix GitHub variable/secret EC2_INSTANCE_ID (expected e.g. i-0652a9c1b9bf2c7dd from terraform output ec2_instance_id)." >&2
            exit 1
          fi
          if [[ "${PING_STATUS}" != "Online" ]]; then
            echo "::error::EC2 is ${EC2_STATE} but SSM status is ${PING_STATUS}. In AWS Console → Systems Manager → Fleet Manager, confirm the instance is Online. Ensure the instance profile includes AmazonSSMManagedInstanceCore and the SSM agent is running." >&2
            exit 1
          fi

          python3 <<'PY' > /tmp/ssm-deploy-params.json
          import json
          import os

          app_path = os.environ["APP_PATH"]
          bucket = os.environ["DEPLOY_BUCKET"]
          jar_key = os.environ["DEPLOY_OBJECT_KEY"]
          script_key = os.environ["DEPLOY_SCRIPT_KEY"]

          commands = [
              "set -euxo pipefail",
              f"mkdir -p {app_path}/incoming {app_path}/scripts {app_path}/app {app_path}/backup {app_path}/logs",
              f"aws s3 cp s3://{bucket}/{jar_key} {app_path}/incoming/gamya-couture.jar.new",
              f"aws s3 cp s3://{bucket}/{script_key} {app_path}/scripts/remote-deploy.sh",
              f"chmod 755 {app_path}/scripts/remote-deploy.sh",
              f"sudo APP_PATH={app_path} bash {app_path}/scripts/remote-deploy.sh",
          ]
          print(json.dumps({"commands": commands}))
          PY

          COMMAND_ID=$(aws ssm send-command \
            --instance-ids "${EC2_INSTANCE_ID}" \
            --document-name "AWS-RunShellScript" \
            --comment "GitHub Actions backend deploy" \
            --parameters file:///tmp/ssm-deploy-params.json \
            --query 'Command.CommandId' --output text)
          echo "SSM command id: ${COMMAND_ID}"

          for attempt in $(seq 1 60); do
            STATUS=$(aws ssm get-command-invocation \
              --command-id "${COMMAND_ID}" \
              --instance-id "${EC2_INSTANCE_ID}" \
              --query 'Status' --output text 2>/dev/null || echo "Pending")
            echo "SSM status: ${STATUS} (attempt ${attempt}/60)"
            case "${STATUS}" in
              Success)
                aws ssm get-command-invocation \
                  --command-id "${COMMAND_ID}" \
                  --instance-id "${EC2_INSTANCE_ID}" \
                  --query '[StandardOutputContent,StandardErrorContent]' \
                  --output text
                exit 0
                ;;
              Failed|Cancelled|TimedOut)
                echo "::group::SSM command output"
                aws ssm get-command-invocation \
                  --command-id "${COMMAND_ID}" \
                  --instance-id "${EC2_INSTANCE_ID}" \
                  --output json
                echo "::endgroup::"
                exit 1
                ;;
            esac
            sleep 10
          done
          echo "SSM deploy timed out" >&2
          exit 1
        env:
          DEPLOY_BUCKET: ${{ steps.deploy_cfg.outputs.deploy_bucket }}
          APP_PATH: ${{ env.APP_PATH }}
          DEPLOY_OBJECT_KEY: ${{ env.DEPLOY_OBJECT_KEY }}
          DEPLOY_SCRIPT_KEY: ${{ env.DEPLOY_SCRIPT_KEY }}

      - name: Verify public health via nginx
        run: |
          EC2_HOST="${{ steps.deploy_cfg.outputs.ec2_host }}"
          HEALTH_URL="http://${EC2_HOST}/actuator/health"
          echo "Checking ${HEALTH_URL}"
          for attempt in $(seq 1 24); do
            RESPONSE="$(curl -sS "$HEALTH_URL" || true)"
            if echo "$RESPONSE" | grep -q '"status":"UP"'; then
              echo "Public health check passed"
              exit 0
            fi
            echo "Attempt ${attempt}/24 — response: ${RESPONSE:-<empty>}"
            sleep 5
          done
          echo "::error::Public health check failed. nginx returned 502 usually means Spring Boot is not running — verify /opt/gamya-couture/config/application.env and systemctl status gamya-couture-backend on EC2." >&2
          exit 1
