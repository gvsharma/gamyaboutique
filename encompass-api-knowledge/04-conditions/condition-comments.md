# Condition comments

Enhanced: manage comments at `/encompass/v3/loans/{loanId}/conditions/{conditionId}/comments`.

Uses the shared comments pattern (`LogCommentContract` on other entities). Webhook Loan `condition` includes `comment` subevent.

Source: loan-enhanced-conditions, manage-comments
