# Loan detail

On click:

1. Redis compact entity
2. Miss: GET Loan `view=entity` + parallel list APIs (milestones, conditions XOR enhanced, documents, conversation logs, tasks)
3. Tabs: `view=logs` for system logs; documents; attachments URLs on demand
4. Do not `view=full` by default

Check `useEnhancedConditionIndicator` before choosing condition APIs.
