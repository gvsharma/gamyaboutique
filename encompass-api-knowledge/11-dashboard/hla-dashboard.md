# HLA dashboard

Same loan index filtered to one HLA. Task Pipeline is **that user’s incomplete tasks**, useful if impersonating the HLA token; a manager token sees the manager’s tasks, not the HLA’s.

Impersonation: documented token-exchange; Super Admin or API user within org. Source: authentication guide.

Persona still applies. **LENDER CONFIGURABLE**
